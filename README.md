# Swift 函数基础 Demo

## 简介

展示 Swift 函数的基本定义和调用。

## 启动和使用

```bash
cd swift-function-basics-demo
swift run
```

## 教程

### 函数定义

```swift
func functionName(parameter: Type) -> ReturnType {
    return value
}
```

### 返回类型

- 使用 `-> Type` 指定返回类型
- 可以返回元组
- 可以返回可选类型 `-> Type?`
